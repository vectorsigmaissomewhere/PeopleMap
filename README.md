"# PeopleMap" 

my-fastapi-project/
├── alembic/                        # Alembic migration scripts (created by alembic init)
│   ├── env.py
│   ├── README
│   ├── script.py.mako
│   └── versions/                   # auto-generated migration files
├── src/                            # All application code (helps with imports & packaging)
│   ├── __init__.py
│   ├── main.py                     # FastAPI() app, lifespan, routers included here
│   ├── config/                     # Settings & env handling
│   │   ├── __init__.py
│   │   └── settings.py             # Pydantic Settings + .env loading
│   ├── database/                   # DB engine, session, base
│   │   ├── __init__.py
│   │   ├── base.py                 # declarative_base() or Base = declarative_base()
│   │   ├── session.py              # get_db() dependency, engine creation
│   │   └── models/                 # ← move shared/base models here if needed
│   ├── core/                       # Shared utilities & security
│   │   ├── __init__.py
│   │   ├── security.py             # password hashing, JWT encode/decode
│   │   └── exceptions.py           # custom HTTP exceptions
│   ├── dependencies/               # Reusable FastAPI dependencies
│   │   └── __init__.py
│   ├── auth/                       # Feature/domain: authentication & users
│   │   ├── __init__.py
│   │   ├── router.py               # APIRouter(prefix="/auth")
│   │   ├── schemas.py              # Pydantic: Login, Token, UserCreate, UserOut...
│   │   ├── models.py               # SQLAlchemy: User table
│   │   ├── crud.py                 # or repository.py – CRUD operations
│   │   ├── service.py              # business logic (validate credentials, etc.)
│   │   └── dependencies.py         # get_current_user, get_current_active_user
│   ├── users/                      # Example: user management (profile, etc.)
│   │   ├── router.py
│   │   ├── schemas.py
│   │   ├── models.py
│   │   └── crud.py
│   └── items/                      # Example domain – add your features here
│       ├── router.py
│       ├── schemas.py
│       ├── models.py
│       └── crud.py
├── tests/                          # pytest structure (mirror src/)
│   ├── __init__.py
│   ├── conftest.py                 # fixtures: client, db session override, etc.
│   ├── test_main.py
│   └── auth/
│       └── test_auth.py
├── .env                            # your secrets (DATABASE_URL, SECRET_KEY...)
├── .env.example                    # template for others
├── alembic.ini                     # Alembic config (autogenerated)
├── requirements.txt                # or pyproject.toml if using uv/poetry
├── .gitignore
├── Dockerfile                      # optional but recommended for prod
└── docker-compose.yml              # local dev with postgres + app



```text 
Make little changes
```